name: Apply Journal Currency Groups

on:
  push:
    branches:
      - audit-import-guards-2026-08-29

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit-import-guards-2026-08-29
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply patch
        run: node patch_journal_currency_groups.cjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/admin/ledger.tsx src/components/JournalEntryCurrencyGroups.tsx patch_journal_currency_groups.cjs
          git diff --cached --quiet || git commit -m "feat: group journal entry details by currency"
          git push origin audit-import-guards-2026-08-29
