# POS / Captain / Oven repair notes

Reference behavior: TableOrdersStore must be the shared local order bridge. POS and Captain save TableOrder records with real subtotal/tax/total/items. Sending to kitchen must call sendOrderToKitchen(orderId), and Oven must subscribe to the same store and filter sentToKitchen && !kitchenCompleted.

Critical POS issue identified in supplied code: `tableOrdersStore.clearOrder(selectedTable)` passes a TableOrder/string mismatch depending on the current store signature. Receipt totals must come from the persisted `invoice` returned by checkout, not from cleared cart state or fallback zeros.

Do not replace large files with partial snippets. Apply surgical edits to the existing POS/Captain/Oven files and preserve existing UI and ERP logic.
